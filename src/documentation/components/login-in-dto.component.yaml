LoginInDTO:
  type: object
  properties:
    email:
      type: string
      format: email
      example: "jhon-doe@email.com"
    password:
      type: string
      example: "SECUREpassword123!"
  required:
    - email
    - password
