get:
  summary: Get all authors
  responses:
    "200":
      description: List of authors
      content:
        application/json:
          schema:
            $ref: "../components/author.component.yaml#/Author"
